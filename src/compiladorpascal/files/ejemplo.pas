Program Example5;
 Var       
    Num1, Num2, Sum1 : Integer;
 Procedure proc(a:integer, b:boolean);
    begin
     a := 3;
     b := true;
     if (3 and true) then
     a := 3
 end;
Begin
    proc(Num1,Num2)
End.